<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos para sincronización con API REST y descarga de imágenes -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo_itanes"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo_itanes"
        android:supportsRtl="true"
        android:theme="@style/Theme.ITANESTourMyApplication"
        android:usesCleartextTraffic="true">

        <!-- Pantalla principal: Listado de los 5 puntos turísticos -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Pantalla de detalle: Información, multimedia, favoritos y ruta -->
        <activity
            android:name=".DetalleActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
    </application>

</manifest>